import 'package:flutter/material.dart';

class FavouriteBooks extends StatelessWidget {
  static const routeName = '/favourite-books';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Favourite Books Screen')),
    );
  }
}
